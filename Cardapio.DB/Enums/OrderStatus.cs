namespace Cardapio.DB.Enums
{
    public enum OrderStatus
    {
        Openned = 1, 
        Closed = 2
    }
    public enum OrderItemStatus
    {
        Pending = 1,
        Preparing = 2,
        Ready =3
    }
}
